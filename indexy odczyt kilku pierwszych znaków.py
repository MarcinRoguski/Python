with open ("indexy.txt", "r", encoding = "UTF-8") as file:
    lines = file.read().splitlines()

#print(lines)  

indexes = []
if index.startswith('WIG') for index in lines:
    
        indexes.append(index)

print(indexes)
